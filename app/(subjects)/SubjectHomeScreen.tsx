import { ScrollView, StyleSheet, Text, View } from 'react-native'
import React from 'react'
import Home from '../../components/Subjects/Home'

export default function HomeScreen() {
  return (
   <ScrollView>
    <Home/>
   </ScrollView>
  )
}

const styles = StyleSheet.create({})